import React, {Component} from 'react';
import PropTypes, {bool, string} from 'prop-types';
import {Botao} from "./index";

class Tabela extends Component {
    render() {
        return (
            <div>
            </div>
        );
    }
}

Tabela.propTypes = {param1: bool};

export default Tabela;