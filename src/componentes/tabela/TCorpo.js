import React from 'react';
import PropTypes from 'prop-types';

const TCorpo = props => {
    return (
        <tbody>

        </tbody>
    );
};

TCorpo.propTypes = {
    
};

export default TCorpo;