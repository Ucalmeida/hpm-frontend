import React from 'react';
import PropTypes from 'prop-types';

const TRodape = props => {
    return (
        <tfoot>

        </tfoot>
    );
};

TRodape.propTypes = {
    
};

export default TRodape;